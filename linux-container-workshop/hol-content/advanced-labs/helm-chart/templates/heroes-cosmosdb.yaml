apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    name: api
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: {{ .Values.rating-api.ports.external }}
    targetPort: {{ .Values.rating-api.ports.external }}
  selector:
    name: {{ .Values.rating-api.name }}
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name:  "{{ .Values.rating-api.name }}-deploy"
  labels:
    name:  {{ .Values.rating-api.name }}
spec:
  replicas: {{ .Values.rating-api.replicaCount }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        name:  {{ .Values.rating-api.name }}
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
      - image: "{{ .Values.rating-api.image.repository }}:{{ .Values.rating-api.image.tag }}"
        name:  "{{ .Values.rating-api.name }}-cntnr"
        resources:
          requests:
            cpu: "20m"
            memory: "55M"
        env:
        - name: MONGODB_HOST
          valueFrom:
            secretKeyRef:
              name: heroes-cosmosdb-secret
              key: host
        - name: MONGODB_PORT
          valueFrom:
            secretKeyRef:
              name: heroes-cosmosdb-secret
              key: port
        - name: MONGODB_USERNAME
          valueFrom:
            secretKeyRef:
              name: heroes-cosmosdb-secret
              key: username
        - name: MONGODB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: heroes-cosmosdb-secret
              key: password
        - name: MONGODB_DBNAME
          value: {{ .Values.mongodb.database.name }}
        ports:
        - containerPort:  {{ .Values.rating-api.ports.internal }}
          name:  {{ .Values.rating-api.name }}
        imagePullPolicy: {{ .Values.rating-api.image.pullPolicy }}
        restartPolicy: {{ .Values.rating-api.restartPolicy }}
---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    name: web
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: {{ .Values.rating-web.ports.external }}
    targetPort: {{ .Values.rating-web.ports.internal }}
  selector:
    name: {{ .Values.rating-web.name }}
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name:  "{{ .Values.rating-web.name }}-deploy"
  labels:
    name:  {{ .Values.rating-web.name }}
spec:
  replicas: {{ .Values.rating-web.replicaCount }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        name:  {{ .Values.rating-web.name }}
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
      - image: "{{ .Values.rating-web.image.repository }}:{{ .Values.rating-web.image.tage }}"
        name:  "{{ .Values.rating-web.name }}-cntnr"
        resources:
          requests:
            cpu: "0.5"
            memory: "1Gi"
        env:
        - name:  API
          value:  "http://api:{{ .Values.rating-api.ports.internal }}/"
        - name: KUBE_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: KUBE_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: KUBE_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort:  {{ .Values.rating-api.ports.internal }}
          name:  {{ .Values.rating-web.name }}
        imagePullPolicy: {{ .Values.rating-web.image.pullPolicy }}
        restartPolicy: {{ .Values.rating-web.restartPolicy }}
---
apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceInstance
metadata:
  name: heroes-cosmosdb-instance
  namespace: default
spec:
  clusterServiceClassExternalName: azure-cosmos-mongo-db
  parameters:
    location: eastus
resourceGroup: heroes-cosmosdb
---
apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: heroes-cosmosdb-binding
  namespace: default
spec:
  instanceRef:
    name: heroes-cosmosdb-instance
  secretName: heroes-cosmosdb-secret
---